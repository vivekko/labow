#include <iostream>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    while (t--)
    {
       int a,b;
       cin>>a;
       cin>>b;
       int ans = SameParity(a,b);
       cout<<ans;
    }
    return 0;
}
int SameParity(int a,int b)
{
    for(int i=0;i<=a/3;i++)
        if(a==())

}
